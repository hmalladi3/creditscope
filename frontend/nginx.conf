server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing: unknown paths fall back to index.html so React Router owns them.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
